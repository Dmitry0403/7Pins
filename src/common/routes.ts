export enum LINKS {
    home = "/",
    details = "/details/:id",
    rules = "/rules",
    start = "/start",
    game = "/game",
    results = "/results",
    setting = "/setting",
    confirmation = "/confirmation",
}
