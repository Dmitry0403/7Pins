export enum LINKS {
    home = "/",
    details = "/details/:id",
    rules = "/rules",
    registration = "/registration",
    game = "/game/:id",
    results = "/results",
    setting = "/setting",
    confirmation = "/confirmation",
    currency = "/currency",
}
