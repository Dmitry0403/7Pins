export enum LINKS {
    home = "/",
    details = "/details/:id",
    rules = "/rules",
    start = "/start",
    game = "/game/:id",
    results = "/results",
    setting = "/setting",
    confirmation = "/confirmation",
}
